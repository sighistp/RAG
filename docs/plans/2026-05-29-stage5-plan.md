# 阶段五实现计划：反思、缓存、清洗、Prompt 管理

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐面试反馈中的 4 个 ⚠️ 缺口——Agent 反思、缓存加固、数据清洗、Prompt 版本管理。

**Architecture:** 4 个独立子系统，每个可单独测试。Agent 反思修改 agent.py；缓存加固增强 resilience.py；数据清洗新建 cleaner.py；Prompt 管理新建 prompt_manager.py + prompts/ 目录。

**Tech Stack:** pybloom_live（布隆过滤器）、chardet（编码检测）、PyYAML（Prompt 文件）、difflib（去重）

---

## 文件结构

| 文件 | 职责 | 操作 |
|------|------|------|
| `rag/agent.py` | 集成工具级反思 + 答案级自检 | 修改 |
| `rag/resilience.py` | 增强 ResultCache（布隆+雪崩+热点） | 修改 |
| `rag/cleaner.py` | 编码检测、乱码清理、去重、元数据提取 | 新建 |
| `rag/loader.py` | 集成 cleaner | 修改 |
| `rag/pipeline.py` | 集成缓存 + 去重 | 修改 |
| `rag/prompt_manager.py` | Prompt 版本管理器 | 新建 |
| `prompts/rewrite.yaml` | 查询改写 prompt | 新建 |
| `prompts/router.yaml` | 路由判断 prompt | 新建 |
| `prompts/agent_system.yaml` | Agent 系统提示 | 新建 |
| `prompts/quality_check.yaml` | 答案自检 prompt | 新建 |
| `rag/query_rewriter.py` | 用 prompt_manager 加载 prompt | 修改 |
| `tests/test_agent.py` | Agent 反思测试 | 修改 |
| `tests/test_resilience.py` | 缓存加固测试 | 修改 |
| `tests/test_cleaner.py` | 数据清洗测试 | 新建 |
| `tests/test_prompt_manager.py` | Prompt 管理测试 | 新建 |

---

## 子系统 A：Agent 反思机制

### Task 1: 工具级反思 — 包装与重试

**Files:**
- Modify: `rag/agent.py`
- Test: `tests/test_agent.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_agent.py (追加)
from unittest.mock import MagicMock


def test_tool_wrapper_retries_on_exception():
    """包装后的工具在异常时自动重试。"""
    from rag.agent import _wrap_tool_with_reflection
    from langchain_core.tools import Tool

    call_count = 0

    def failing_func(inp):
        nonlocal call_count
        call_count += 1
        if call_count == 1:
            raise ValueError("transient error")
        return "success"

    tool = Tool(name="test_tool", description="test", func=failing_func)
    wrapped = _wrap_tool_with_reflection(tool)

    result = wrapped.func("input")
    assert result == "success"
    assert call_count == 2


def test_tool_wrapper_returns_error_after_retry_fails():
    """重试仍失败时返回错误信息而非抛异常。"""
    from rag.agent import _wrap_tool_with_reflection
    from langchain_core.tools import Tool

    def always_fail(inp):
        raise ValueError("permanent error")

    tool = Tool(name="test_tool", description="test", func=always_fail)
    wrapped = _wrap_tool_with_reflection(tool)

    result = wrapped.func("input")
    assert "error" in result.lower() or "失败" in result


def test_tool_wrapper_passes_through_on_success():
    """正常调用不触发重试。"""
    from rag.agent import _wrap_tool_with_reflection
    from langchain_core.tools import Tool

    tool = Tool(name="test_tool", description="test", func=lambda x: f"result: {x}")
    wrapped = _wrap_tool_with_reflection(tool)

    result = wrapped.func("hello")
    assert result == "result: hello"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_agent.py::test_tool_wrapper_retries_on_exception -v`
Expected: FAIL with "cannot import name '_wrap_tool_with_reflection'"

- [ ] **Step 3: Write minimal implementation**

在 `rag/agent.py` 末尾追加：

```python
def _is_empty_or_error(result: str) -> bool:
    """判断工具结果是否为空或错误。"""
    if not result or not result.strip():
        return True
    error_indicators = ["error", "错误", "失败", "exception", "traceback"]
    return any(ind in result.lower() for ind in error_indicators)


def _wrap_tool_with_reflection(tool: "Tool") -> "Tool":
    """包装工具，失败时自动重试一次。"""
    original_func = tool.func
    tool_name = tool.name

    def reflective_func(inp: str) -> str:
        try:
            result = original_func(inp)
        except Exception as e:
            # 第一次异常，尝试重试
            try:
                return original_func(inp)
            except Exception:
                return f"工具 {tool_name} 执行失败: {e}"
        if _is_empty_or_error(result):
            # 空结果，尝试重试
            try:
                retry_result = original_func(inp)
                if not _is_empty_or_error(retry_result):
                    return retry_result
            except Exception:
                pass
        return result

    tool.func = reflective_func
    return tool
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_agent.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/agent.py tests/test_agent.py
git commit -m "feat: add tool-level reflection wrapper with retry"
```

---

### Task 2: 答案级自检

**Files:**
- Modify: `rag/agent.py`
- Test: `tests/test_agent.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_agent.py (追加)
from unittest.mock import patch


def test_check_answer_quality_pass():
    """自检通过时不重跑。"""
    from rag.agent import _check_answer_quality

    with patch("rag.agent.generate", return_value='{"verdict": "pass", "missing": []}'):
        result = _check_answer_quality("什么是 Raft？", "Raft 是一种一致性协议")
        assert result["verdict"] == "pass"


def test_check_answer_quality_fail():
    """自检失败时返回缺失要点。"""
    from rag.agent import _check_answer_quality

    with patch("rag.agent.generate", return_value='{"verdict": "fail", "missing": ["选举机制"]}'):
        result = _check_answer_quality("什么是 Raft？", "Raft 是一种协议")
        assert result["verdict"] == "fail"
        assert "选举机制" in result["missing"]


def test_agent_run_reflects_on_answer():
    """Agent.run() 在答案不完整时自动重跑。"""
    from rag.agent import RAGAgent

    with patch("rag.agent.create_agent") as mock_create_agent:
        mock_agent = MagicMock()
        # 第一次返回不完整答案，第二次返回完整答案
        mock_agent.invoke.side_effect = [
            {"messages": [MagicMock(content="Raft 是一种协议")]},
            {"messages": [MagicMock(content="Raft 是一种一致性协议，包含选举机制")]},
        ]
        mock_create_agent.return_value = mock_agent

        with patch("rag.agent._check_answer_quality") as mock_check:
            mock_check.side_effect = [
                {"verdict": "fail", "missing": ["选举机制"]},
                {"verdict": "pass", "missing": []},
            ]
            agent = RAGAgent(retriever=None)
            result = agent.run("什么是 Raft？")
            assert "选举机制" in result
            assert mock_agent.invoke.call_count == 2
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_agent.py::test_check_answer_quality_pass -v`
Expected: FAIL with "cannot import name '_check_answer_quality'"

- [ ] **Step 3: Write minimal implementation**

在 `rag/agent.py` 追加：

```python
import json as _json

QUALITY_CHECK_PROMPT = """你是一个答案质量审查员。请判断以下答案是否完整回答了用户的问题。

问题：{question}
答案：{answer}

判断标准：
1. 答案是否直接回应了问题的核心诉求
2. 是否遗漏了问题中的关键信息点
3. 是否包含无关或错误的信息

请用 JSON 格式回复：
{{"verdict": "pass 或 fail", "missing": ["缺失的要点1", ...]}}"""


def _check_answer_quality(question: str, answer: str) -> dict:
    """用 LLM 自检答案质量。"""
    prompt = QUALITY_CHECK_PROMPT.format(question=question, answer=answer)
    messages = [
        {"role": "system", "content": "你是一个答案质量审查员。"},
        {"role": "user", "content": prompt},
    ]
    result = generate(messages)
    try:
        return _json.loads(result)
    except _json.JSONDecodeError:
        return {"verdict": "pass", "missing": []}
```

修改 `RAGAgent.run()` 方法，加入自检循环：

```python
def run(self, question: str) -> str:
    """Run the agent on a question with answer quality reflection."""
    from langchain_core.messages import HumanMessage

    max_reflection = 2
    current_question = question

    for round_num in range(max_reflection + 1):
        result = self.agent.invoke(
            {"messages": [HumanMessage(content=current_question)]},
            config={"recursion_limit": self.max_iterations * 2},
        )
        messages = result.get("messages", [])
        answer = messages[-1].content if messages else "未能生成回答"

        if round_num < max_reflection:
            check = _check_answer_quality(question, answer)
            if check.get("verdict") == "pass":
                return answer
            missing = check.get("missing", [])
            if missing:
                current_question = f"你之前的回答不完整，缺少以下要点：{missing}。请重新回答原始问题：{question}"
            else:
                return answer
        else:
            return answer

    return answer
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_agent.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/agent.py tests/test_agent.py
git commit -m "feat: add answer-level quality reflection with LLM self-check"
```

---

## 子系统 B：缓存加固

### Task 3: 布隆过滤器防穿透

**Files:**
- Modify: `rag/resilience.py`
- Test: `tests/test_resilience.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_resilience.py (追加)
from rag.resilience import ResultCache


def test_bloom_filter_blocks_unknown_keys():
    """未 set 过的 key 被布隆过滤器拦截，不查 dict。"""
    cache = ResultCache(ttl_seconds=1.0)
    # 从未 set 过 "unknown_question"
    assert cache.get("unknown_question") is None


def test_bloom_filter_allows_known_keys():
    """set 过的 key 可以通过布隆过滤器。"""
    cache = ResultCache(ttl_seconds=1.0)
    cache.set("known_question", "answer")
    assert cache.get("known_question") == "answer"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_resilience.py::test_bloom_filter_blocks_unknown_keys -v`
Expected: 可能 PASS（因为缓存 miss 本身就返回 None），需要调整测试方式

- [ ] **Step 3: Implement**

修改 `rag/resilience.py` 的 `ResultCache`，添加布隆过滤器：

```python
import hashlib
import threading
import time
import random
from bitarray import bitarray


class BloomFilter:
    """简单布隆过滤器，防缓存穿透。"""

    def __init__(self, capacity: int = 10000, error_rate: float = 0.01):
        self.capacity = capacity
        self.error_rate = error_rate
        self.size = int(-capacity * (error_rate ** (1.0 / (2.0 * (2.0 ** 0.5)))))
        self.hash_count = max(1, int(self.size / capacity * 0.693))
        self._bits = bitarray(self.size)
        self._bits.setall(0)

    def _hashes(self, key: str):
        h1 = int(hashlib.md5(key.encode()).hexdigest(), 16)
        h2 = int(hashlib.sha256(key.encode()).hexdigest(), 16)
        for i in range(self.hash_count):
            yield (h1 + i * h2) % self.size

    def add(self, key: str):
        for pos in self._hashes(key):
            self._bits[pos] = 1

    def __contains__(self, key: str) -> bool:
        return all(self._bits[pos] for pos in self._hashes(key))
```

修改 `ResultCache.__init__` 和 `get`/`set`：

```python
class ResultCache:
    """缓存：防穿透（布隆）+ 防雪崩（TTL 抖动）+ 防热点（热点永不过期）。"""

    def __init__(self, ttl_seconds: float = 300.0, max_size: int = 1000, hot_threshold: int = 10):
        self.ttl = ttl_seconds
        self.max_size = max_size
        self.hot_threshold = hot_threshold
        self._store: dict[str, tuple[str, float]] = {}
        self._access_count: dict[str, int] = {}
        self._stale_keys: set[str] = set()
        self._bloom = BloomFilter(capacity=max_size * 2)
        self._lock = threading.Lock()

    def _key(self, question: str) -> str:
        return hashlib.md5(question.encode()).hexdigest()

    def get(self, question: str):
        key = self._key(question)
        if key not in self._bloom:
            return None
        with self._lock:
            self._access_count[key] = self._access_count.get(key, 0) + 1
            if key in self._store:
                answer, expire_time = self._store[key]
                if time.time() < expire_time:
                    return answer
                if self._access_count[key] >= self.hot_threshold:
                    self._stale_keys.add(key)
                    return answer
                del self._store[key]
        return None

    def set(self, question: str, answer: str):
        key = self._key(question)
        self._bloom.add(key)
        with self._lock:
            if len(self._store) >= self.max_size:
                oldest_key = min(self._store, key=lambda k: self._store[k][1])
                del self._store[oldest_key]
            jitter = random.uniform(-0.1, 0.1) * self.ttl
            self._store[key] = (answer, time.time() + self.ttl + jitter)

    def get_stale_keys(self) -> set[str]:
        """返回需要刷新的热点 key。"""
        with self._lock:
            stale = self._stale_keys.copy()
            self._stale_keys.clear()
        return stale
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_resilience.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/resilience.py tests/test_resilience.py
git commit -m "feat: cache hardening with bloom filter, TTL jitter, hot key protection"
```

---

### Task 4: 缓存集成到 pipeline

**Files:**
- Modify: `rag/pipeline.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_resilience.py (追加)
from unittest.mock import patch, MagicMock


def test_pipeline_uses_cache():
    """相同问题第二次查询应命中缓存。"""
    from rag.pipeline import RAGPipeline

    with patch("rag.pipeline.load"), \
         patch("rag.pipeline.chunk", return_value=[]), \
         patch("rag.pipeline.embed", return_value=[]), \
         patch("rag.pipeline.clear"), \
         patch("rag.pipeline.add"), \
         patch("rag.pipeline.Retriever"), \
         patch("rag.pipeline.Reranker"), \
         patch("rag.pipeline.ExecutionTracker"):
        pipeline = RAGPipeline("test.txt", memory_db_path=":memory:")
        # 模拟查询
        pipeline._cache = MagicMock()
        pipeline._cache.get.return_value = "cached answer"
        result = pipeline.query("test question")
        assert result.answer == "cached answer"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_resilience.py::test_pipeline_uses_cache -v`

- [ ] **Step 3: Implement**

修改 `rag/pipeline.py` 的 `query()` 方法，在 guard 检查后加入缓存逻辑：

```python
from rag.resilience import ResultCache

# 在 __init__ 中
self._cache = ResultCache()

# 在 query() 中，guard 检查之后
cached = self._cache.get(question)
if cached:
    return QueryResult(answer=cached, context=[], sources=[{"doc_name": "缓存", "chunk_index": 0}])

# 在返回前
self._cache.set(question, answer)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_resilience.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/pipeline.py tests/test_resilience.py
git commit -m "feat: integrate hardened cache into pipeline"
```

---

## 子系统 C：数据清洗

### Task 5: 编码检测 + 乱码清理

**Files:**
- Create: `rag/cleaner.py`
- Test: `tests/test_cleaner.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_cleaner.py
from rag.cleaner import detect_and_decode, clean_text


def test_detect_and_decode_gbk():
    """GBK 编码自动检测并正确解码。"""
    raw = "你好世界".encode("gbk")
    result = detect_and_decode(raw)
    assert result == "你好世界"


def test_detect_and_decode_utf8():
    """UTF-8 编码正确解码。"""
    raw = "Hello World".encode("utf-8")
    result = detect_and_decode(raw)
    assert result == "Hello World"


def test_clean_text_removes_bom():
    """BOM 字符被移除。"""
    result = clean_text("﻿Hello")
    assert "﻿" not in result
    assert result == "Hello"


def test_clean_text_removes_zero_width():
    """零宽空格被移除。"""
    result = clean_text("Hello​World")
    assert "​" not in result
    assert result == "HelloWorld"


def test_clean_text_collapses_blank_lines():
    """连续空行合并为两个换行。"""
    result = clean_text("line1\n\n\n\nline2")
    assert result == "line1\n\nline2"


def test_clean_text_preserves_content():
    """正常内容不受影响。"""
    text = "这是一个正常的文档。\n包含多行内容。"
    result = clean_text(text)
    assert result == text
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_cleaner.py -v`
Expected: FAIL with "ModuleNotFoundError: No module named 'rag.cleaner'"

- [ ] **Step 3: Write minimal implementation**

```python
# rag/cleaner.py
"""数据清洗管道 — 编码检测、乱码清理、去重、元数据提取。"""
import hashlib
import re
from difflib import SequenceMatcher

import chardet


def detect_and_decode(raw_bytes: bytes) -> str:
    """自动检测编码并解码。"""
    detected = chardet.detect(raw_bytes)
    encoding = detected.get("encoding", "utf-8") or "utf-8"
    try:
        return raw_bytes.decode(encoding)
    except (UnicodeDecodeError, LookupError):
        return raw_bytes.decode("utf-8", errors="replace")


def clean_text(text: str) -> str:
    """清理特殊字符：BOM、零宽空格、不间断空格、控制字符。"""
    text = text.replace("﻿", "")  # BOM
    text = text.replace("​", "")  # 零宽空格
    text = text.replace(" ", " ")  # 不间断空格
    text = "".join(c for c in text if c in "\n\t" or ord(c) >= 32)
    text = re.sub(r"\n{3,}", "\n\n", text)
    return text.strip()


def document_hash(text: str) -> str:
    """文档级 MD5 hash，用于去重。"""
    return hashlib.md5(text.encode("utf-8")).hexdigest()


def deduplicate_chunks(chunks: list[str], threshold: float = 0.95) -> list[str]:
    """段落级去重，相似度超过阈值的段落被去除。"""
    unique = []
    for chunk in chunks:
        if not any(_similarity(chunk, u) > threshold for u in unique):
            unique.append(chunk)
    return unique


def _similarity(a: str, b: str) -> float:
    """计算两个字符串的相似度。"""
    return SequenceMatcher(None, a, b).ratio()


def extract_metadata(text: str) -> dict:
    """提取文档元数据：标题、作者、日期。"""
    metadata = {}
    title_match = re.search(r"^#\s+(.+)$", text, re.MULTILINE)
    if title_match:
        metadata["title"] = title_match.group(1).strip()
    author_match = re.search(r"(?:作者|Author)[：:]\s*(.+)", text)
    if author_match:
        metadata["author"] = author_match.group(1).strip()
    date_match = re.search(r"(?:日期|Date)[：:]\s*(\d{4}[-/]\d{1,2}[-/]\d{1,2})", text)
    if date_match:
        metadata["date"] = date_match.group(1).strip()
    return metadata


def clean_document(text: str) -> tuple[str, dict]:
    """完整清洗流程：清理 + 元数据提取。返回 (清洗后文本, 元数据)。"""
    cleaned = clean_text(text)
    metadata = extract_metadata(cleaned)
    return cleaned, metadata
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_cleaner.py -v`
Expected: 6 PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/cleaner.py tests/test_cleaner.py
git commit -m "feat: add data cleaning pipeline (encoding, sanitize, dedup, metadata)"
```

---

### Task 6: 集成 cleaner 到 loader/pipeline

**Files:**
- Modify: `rag/loader.py`
- Modify: `rag/pipeline.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_cleaner.py (追加)
from unittest.mock import patch


def test_loader_uses_cleaner():
    """loader 加载后应调用 cleaner。"""
    from rag.loader import load

    with patch("rag.loader.clean_text", return_value="cleaned") as mock_clean:
        result = load("test.txt")
        mock_clean.assert_called()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_cleaner.py::test_loader_uses_cleaner -v`

- [ ] **Step 3: Implement**

修改 `rag/loader.py`，在 `load_text` 和 `load_markdown` 中加入 `clean_text` 调用：

```python
from rag.cleaner import clean_text

def load_text(path: str) -> str:
    # ... 原有逻辑
    text = ...
    return clean_text(text)
```

修改 `rag/pipeline.py`，在分块后加入去重：

```python
from rag.cleaner import deduplicate_chunks

# 在 chunk() 之后
chunk_texts = [c.text for c in chunks]
unique_texts = deduplicate_chunks(chunk_texts)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_cleaner.py tests/test_loader.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/loader.py rag/pipeline.py tests/test_cleaner.py
git commit -m "feat: integrate cleaner into loader and pipeline"
```

---

## 子系统 D：Prompt 版本管理

### Task 7: PromptManager 核心

**Files:**
- Create: `rag/prompt_manager.py`
- Create: `prompts/rewrite.yaml`
- Create: `prompts/router.yaml`
- Create: `prompts/agent_system.yaml`
- Create: `prompts/quality_check.yaml`
- Test: `tests/test_prompt_manager.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_prompt_manager.py
import os
import tempfile
import pytest
from rag.prompt_manager import PromptManager


@pytest.fixture
def pm(tmp_path):
    """创建带测试 prompt 的临时目录。"""
    prompt_dir = tmp_path / "prompts"
    prompt_dir.mkdir()
    (prompt_dir / "test.yaml").write_text(
        "name: test\nversion: 2\ndescription: test prompt\n"
        "changelog:\n  - version: 2\n    date: 2026-05-29\n    change: v2\n"
        "  - version: 1\n    date: 2026-05-23\n    change: v1\n"
        "template: |\n  Hello {name}, version {version}\n",
        encoding="utf-8",
    )
    (prompt_dir / "test_v1.yaml").write_text(
        "name: test\nversion: 1\ndescription: test prompt v1\n"
        "changelog:\n  - version: 1\n    date: 2026-05-23\n    change: v1\n"
        "template: |\n  Hi {name}\n",
        encoding="utf-8",
    )
    return PromptManager(str(prompt_dir))


def test_get_latest_version(pm):
    """version=None 加载最新版本。"""
    template = pm.get("test")
    assert "Hello" in template


def test_get_specific_version(pm):
    """指定版本加载。"""
    template = pm.get("test", version=1)
    assert "Hi" in template


def test_render(pm):
    """模板变量渲染。"""
    result = pm.render("test", name="World")
    assert "Hello World" in result


def test_list_versions(pm):
    """列出所有版本。"""
    versions = pm.list_versions("test")
    assert len(versions) == 2
    assert versions[0]["version"] == 2


def test_not_found(pm):
    """不存在的 prompt 抛出错误。"""
    with pytest.raises(FileNotFoundError):
        pm.get("nonexistent")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_prompt_manager.py -v`
Expected: FAIL with "ModuleNotFoundError"

- [ ] **Step 3: Write minimal implementation**

```python
# rag/prompt_manager.py
"""Prompt 版本管理器 — YAML 文件化、版本化、模板变量。"""
import os
import yaml


class PromptManager:
    def __init__(self, prompts_dir: str = "prompts"):
        self.prompts_dir = prompts_dir

    def _load_all_versions(self, name: str) -> list[dict]:
        """加载指定 prompt 的所有版本文件。"""
        versions = []
        for fname in os.listdir(self.prompts_dir):
            if fname.endswith(".yaml") and name in fname:
                path = os.path.join(self.prompts_dir, fname)
                with open(path, "r", encoding="utf-8") as f:
                    data = yaml.safe_load(f)
                    if data.get("name") == name:
                        versions.append(data)
        if not versions:
            raise FileNotFoundError(f"Prompt '{name}' not found in {self.prompts_dir}")
        return sorted(versions, key=lambda x: x.get("version", 0))

    def get(self, name: str, version: int | None = None) -> str:
        """加载 prompt 模板。version=None 时加载最新版本。"""
        all_versions = self._load_all_versions(name)
        if version is not None:
            for v in all_versions:
                if v.get("version") == version:
                    return v["template"]
            raise ValueError(f"Version {version} not found for prompt '{name}'")
        return all_versions[-1]["template"]

    def render(self, name: str, version: int | None = None, **kwargs) -> str:
        """加载并渲染模板变量。"""
        template = self.get(name, version=version)
        return template.format(**kwargs)

    def list_versions(self, name: str) -> list[dict]:
        """列出 prompt 的所有版本信息。"""
        all_versions = self._load_all_versions(name)
        return [
            {"version": v.get("version"), "date": v.get("changelog", [{}])[0].get("date", ""), "change": v.get("changelog", [{}])[0].get("change", "")}
            for v in reversed(all_versions)
        ]
```

创建 `prompts/` 目录和 4 个 prompt 文件。

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_prompt_manager.py -v`
Expected: 5 PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/prompt_manager.py prompts/ tests/test_prompt_manager.py
git commit -m "feat: prompt manager with YAML versioning and template rendering"
```

---

### Task 8: 集成 PromptManager 到 query_rewriter/agent

**Files:**
- Modify: `rag/query_rewriter.py`
- Modify: `rag/agent.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_prompt_manager.py (追加)
from unittest.mock import patch


def test_query_rewriter_uses_prompt_manager():
    """query_rewriter 应从 prompt_manager 加载 prompt。"""
    from rag.query_rewriter import rewrite_query

    with patch("rag.prompt_manager.PromptManager") as MockPM:
        mock_pm = MockPM.return_value
        mock_pm.render.return_value = "改写：{question}"
        with patch("rag.generator.generate", return_value="改写结果"):
            result = rewrite_query("测试问题")
            assert result == "改写结果"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_prompt_manager.py::test_query_rewriter_uses_prompt_manager -v`

- [ ] **Step 3: Implement**

修改 `rag/query_rewriter.py`：

```python
from rag.prompt_manager import PromptManager

_pm = PromptManager()

def rewrite_query(question: str) -> str:
    prompt = _pm.render("rewrite", question=question)
    messages = [
        {"role": "system", "content": "你是一个查询改写助手。"},
        {"role": "user", "content": prompt},
    ]
    result = generate(messages).strip()
    return result if result else question
```

修改 `rag/agent.py` 的 `ROUTER_PROMPT` 和 Agent system prompt，改为从 prompt_manager 加载。

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_prompt_manager.py tests/test_agent.py -v`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
git add rag/query_rewriter.py rag/agent.py tests/test_prompt_manager.py
git commit -m "feat: integrate prompt manager into query_rewriter and agent"
```

---

### Task 9: 全量回归 + 文档更新

- [ ] **Step 1: Run full test suite**

Run: `python -m pytest tests/ -v --tb=short`
Expected: ALL PASSED

- [ ] **Step 2: Update plan document**

在 `docs/superpowers/plans/rag-system-plan.md` 中更新 Task 32-35 状态。

- [ ] **Step 3: Update dev log**

在 `docs/superpowers/plans/dev-log.md` 中记录阶段五实现。

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/plans/
git commit -m "docs: update plan and dev-log with stage 5 completion"
```
